import { RequestBodyValidationPipePipe } from './request-body-validation-pipe.pipe';

describe('RequestBodyValidationPipePipe', () => {
  it('should be defined', () => {
    expect(new RequestBodyValidationPipePipe()).toBeDefined();
  });
});
